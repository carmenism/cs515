/* this file gives the amount charged for instate and out of
 * state calls, as well as the service charge.
*/

const float instateCharge = 0.10;
    // per minute charge for non-local instate calls

const float outstateCharge = 0.25;
    // per minute charge for out of state calls

const float serviceCharge = 5.00;
    // monthly (billing period) service charge, charged to all
    // accounts
